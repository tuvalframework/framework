/* import { timer as _timer } from 'rxjs';
import { TObservable } from './TSubject';

export class Observables {
    public static Timer(dueTime?: number | Date, periodOrScheduler?: number ): TObservable<number> {
        return _timer(dueTime, periodOrScheduler);
    }
}
 */