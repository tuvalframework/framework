
export class SystemInformation {
    public static VerticalScrollBarWidth: number = 20;
    public static HorizontalScrollBarHeight: number = 20;
    public static Border3DSize: { width: number, height: number } = { width: 1, height: 1 };
    public static BorderSize: { width: number, height: number } = { width: 1, height: 1 };
}